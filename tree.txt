📁 Project_Root
│
├── 🎯 main.py
│   └── Точка входа для консольного запуска.
│       └── main() — запрашивает пути и вызывает build_cable_database()
│
├── 🖥️ cable_master.py
│   └── Графический интерфейс (GUI) с двумя вкладками.
│       ├── __init__() — инициализация окна и путей
│       ├── show_about() — окно "О программе"
│       ├── setup_ui() — создание интерфейса
│       ├── setup_parser_tab() — настройка вкладки "Парсер"
│       ├── setup_matcher_tab() — настройка вкладки "Матчер"
│       ├── select_journals_dir() — выбор папки с журналами
│       ├── select_output_dir() — выбор папки сохранения
│       ├── run_parser() — запуск парсера в потоке
│       ├── _process_parser() — вызов build_cable_database()
│       ├── _on_parser_success() / _on_parser_error()
│       ├── select_matcher_db() — выбор базы
│       ├── run_matcher() — запуск матчера в потоке
│       ├── _process_matcher() — вызов process_multiple_journals()
│       ├── _on_matcher_success() / _on_matcher_error()
│       ├── open_result_folder() — открыть папку (Парсер)
│       ├── open_matcher_result_folder() — открыть папку (Матчер)
│       ├── open_folder_path() — открыть папку в проводнике
│       ├── update_status() — обновление статуса
│       ├── update_progress() — обновление прогресс-бара
│       └── run() — главный цикл Tkinter
│
├── 🧠 excel_utils.py
│   └── Создание Excel-базы из журналов.
│       ├── current_version_finder() — поиск последней версии базы
│       ├── remove_plus_from_numbers() — удаление '+' из чисел
│       ├── mark_merged_cables() — отметка "Объединён"
│       ├── copy_merged_sheet_from_old_base() — копирование листа
│       ├── load_old_notes() — загрузка примечаний
│       ├── check_start_end_trace() — проверка трассы
│       ├── create_log_sheet() — создание листа "Лог"
│       ├── archive_old_base() — архивация старой базы
│       ├── archive_processed_journals() — архивация журналов
│       └── build_cable_database() — ⭐ ГЛАВНАЯ: сборка базы
│
├── 🔧 cable_parser.py
│   └── Парсинг строки кабельного журнала.
│       ├── is_number() — проверка на число
│       ├── parse_coordinates_from_list() — извлечение X,Y,Z
│       ├── parse_kks_block() — разбор блока "Откуда/Куда"
│       ├── merge_optical_cable_mark() — склейка оптических марок
│       └── row_parser() — ⭐ ГЛАВНАЯ: парсинг строки → 20 полей
│
├── 🔍 cable_matcher.py
│   └── Поиск ответных частей кабелей.
│       ├── get_column_index() — индекс колонки по названию
│       ├── get_journal_info() — источник и дата журнала
│       ├── find_all_matching_cables() — поиск всех кабелей с ККС
│       ├── compare_room_equip_pairs() — сравнение "Откуда/Куда"
│       ├── extract_trace_elements() — извлечение regular_cable_tray
│       ├── combine_traces() — объединение трасс с разворотом
│       ├── create_worksheet() — лист "В работу"
│       ├── process_journal() — ⭐ ГЛАВНАЯ: обработка журнала
│       └── process_multiple_journals() — обработка нескольких журналов
│
├── 🧩 kks_utils.py
│   └── Работа с KKS и координатами.
│       ├── cleanCyrFromLat() — замена кириллицы на латиницу
│       ├── parse_kks_room_and_equip() — разделение KKS
│       ├── parse_route_to_bounds() — координаты здания из маршрута
│       ├── export_kks_to_json() — экспорт границ зданий в JSON
│       ├── parse_coordinate_pair() — парсинг координат
│       ├── check_and_swap_axes() — проверка и смена осей
│       ├── format_coordinate() — форматирование координаты
│       └── normalize_coordinates() — нормализация координат
│
├── 📄 read_docx.py
│   └── Чтение Word-журналов (.docx, .doc).
│       ├── make_hashable() — преобразование в хешируемый вид
│       ├── is_subset_with_lists() — проверка подмножества
│       ├── all_deep_empty() — проверка пустоты
│       ├── get_mismatch_indices() — несовпадающие индексы
│       ├── convert_doc_to_docx() — конвертация .doc → .docx
│       ├── convert_numbering_to_text() — нумерация в текст (COM)
│       ├── move_file() — перемещение файла
│       ├── take_all_docx_from_dir() — сбор .docx и конвертация .doc
│       ├── extract_all_text_from_cell() — текст из ячейки (рекурсивно)
│       ├── remove_duplicate_pairs() — удаление дублей
│       ├── remove_duplicates() — удаление последовательных дублей
│       ├── find_first_and_last_sublist_index() — поиск подсписка
│       ├── extract_all_text_from_table() — данные из таблицы Word
│       └── extract_all_text_from_docx() — ⭐ ГЛАВНАЯ: данные из .docx
│
├── 📊 read_xlsx.py
│   └── Чтение Excel-журналов (.xlsx).
│       ├── all_deep_empty() — проверка пустоты
│       ├── move_file() — перемещение файла
│       ├── take_all_xlsx_from_dir() — сбор .xlsx
│       ├── extract_cell_text() — текст из ячейки
│       ├── find_data_start_row() — поиск первой строки с номером
│       ├── extract_all_text_from_xlsx() — ⭐ ГЛАВНАЯ: данные из .xlsx
│       └── process_cable_rows() — обработка строк одного кабеля
│
├── ⚙️ config.py
│   └── Глобальные настройки и регулярные выражения.
│       ├── BASE_NAME — имя базы
│       ├── regular_journal_kks / regular_journal_kks_short — ККС журнала
│       ├── regular_filename — парсинг имени файла
│       ├── regular_num — номер кабеля
│       ├── regular_cableSection — сечение
│       ├── regular_cableMark — марка
│       ├── arrayCableMarks — список марок
│       ├── regular_KKS_room / building / equipment — KKS
│       ├── regular_cable_tray — ККС конструкций
│       └── HEADERS — структура Excel-таблицы
│
├── 🖼️ icon.ico
│   └── Иконка программы
│
│
├── 🗺️ KKS_building_bounds.json
│   └── Границы зданий для нормализации координат
